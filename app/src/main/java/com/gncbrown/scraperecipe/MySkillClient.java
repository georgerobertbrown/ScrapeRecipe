package com.gncbrown.scraperecipe;

//import com.amazon.ask.Skills;
//import com.amazon.alexa.ask.sdk.SkillRequest;
//import com.amazon.alexa.ask.sdk.SkillResponse;

import android.app.Activity;
import android.content.Intent;
import android.os.Bundle;


public class MySkillClient  //extends SkillClient {
{

/*
    private ShoppingList shoppingList;

    public MySkillClient(AskSdk sdk) {
        super(sdk);
    }

    @Override
    public void onConnected() {
        // Get the user's shopping list
        shoppingList = sdk.getServiceClient().getShoppingList();
    }

    @Override
    public void onDisconnected() {
        // Disconnect from the Alexa service
        sdk.disconnect();
    }

    public ShoppingList getShoppingList() {
        return shoppingList;
    }

 */
}